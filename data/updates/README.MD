All updates in this file should be arrays, containing strings of SQL to run.

For example:

--------------------------

[
    "ALTER TABLE something",
    "UPDATE something SET something = 'something' WHERE something"
]